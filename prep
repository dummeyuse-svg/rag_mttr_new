# Step 1 (ONLY ONCE)
python clean_excel.py --file "MTDR Records.xlsx"

# Step 2 (Daily use)
uvicorn app:app --host 127.0.0.1 --port 8000

1. File is in same folder (simplest)

If your structure is:

project/
├── clean_excel.py
├── MTDR Records.xlsx

Then run:

python clean_excel.py --file "MTDR Records.xlsx"

✔ Works directly



pip install fastapi uvicorn pandas openpyxl chromadb sentence-transformers httpx


📍 Correct setup order (very important)
Step 1 — Install Ollama

(download from official site)

Step 2 — Pull the model (THIS STEP 👇)
ollama pull llama3

👉 This downloads the AI model locally
👉 Size ~4–8 GB (depends on version)

Step 3 — Prepare your data
python clean_excel.py --file "MTDR Records.xlsx"
Step 4 — Start your system
start.bat


MTDR_AI/
├── app.py
├── clean_excel.py
├── index.html
├── start.bat
└── data/
    └── MTDR Records.xlsx


Start your system (EVERY TIME YOU USE)

This uses:

👉 start.bat

Just double-click OR run:

start.bat
